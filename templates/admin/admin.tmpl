<!--
  vim: filetype=html
-->
{{define "admin"}}
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="/assets/js/htmx.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/assets/css/admin/admin.css">
    <link href="/assets/logo.png" rel="shortcut icon" type="image/x-icon" />
  </head>
  <body>
    <div class="admin-container">
      <!-- Sidebar -->
      <nav class="admin-sidebar">
        <div class="sidebar-header">
          <h2 onclick="window.location.href='/admin'">Dashboard</h2>
        </div>

        <div class="nav-links">
          <a href="/admin/users" class="nav-item" hx-get="/admin/users" hx-target="#content" hx-push-url="true">
            <i class="fas fa-user"></i> Users
          </a>
          <a href="/admin/categories" class="nav-item" hx-get="/admin/categories" hx-target="#content" hx-push-url="true">
            <i class="fas fa-list"></i> Categories
          </a>
          <a href="/admin/tags" class="nav-item" hx-get="/admin/tags" hx-target="#content" hx-push-url="true">
            <i class="fas fa-tag"></i> Tags
          </a>
          <a href="/admin/posts" class="nav-item" hx-get="/admin/posts" hx-target="#content" hx-push-url="true">
            <i class="fas fa-file-alt"></i> Posts
          </a>
          <a href="/admin/images" class="nav-item" hx-get="/admin/images" hx-target="#content" hx-push-url="true">
            <i class="fas fa-image"></i> Images
          </a>
        </div>

        <div class="sidebar-footer">
          <a href="/admin/settings" class="nav-item" hx-get="/admin/settings" hx-target="#content" hx-push-url="true">
            <i class="fas fa-cog"></i> Site Settings
          </a>
          <form id="logoutForm" action="/logout" method="POST" class="nav-item">
              <button type="submit" class="btn-logout">
                  <i class="fas fa-sign-out-alt"></i> Logout
              </button>
          </form>
        </div>
      </nav>

      <!-- Main Content -->
      <main class="admin-main" id="content">
        <div id="errorPopup" class="error-popup">
          <i class="fas fa-exclamation-circle error-icon"></i>
          <div class="error-content">
            <p id="errorMessage"></p>
          </div>
          <span class="close-error" onclick="closeError()">&times;</span>
        </div>
        {{block "content" .}}{{end}}
      </main>
    </div>

    <script src="/assets/js/script.js"></script>
  </body>
</html>
{{end}}
